#include <stdint.h>
#include <string.h>

#include <stdio.h>

#define AES_BLOCK_SIZE 16
#define AES_ROUNDS 10
#define AES_KEY_LEN 16
#define WORD_LEN 4

const uint8_t SBOX[] = {
  ,
};
const uint8_t INV_SBOX[] = {
  ,
};
uint8_t Rcon[11] = {
    0x00,
    0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1B, 0x36
};
void RotWord(uint8_t word[WORD_LEN]){
    uint8_t temp = word[0];
    for(int i = 1; i < WORD_LEN; i++){
        word[i - 1] = word[i];
    }
    word[WORD_LEN - 1] = temp;
}

void SubWord(uint8_t word[WORD_LEN]){
    for (size_t i = 0; i < WORD_LEN; i++){
        word[i] = SBOX[word[i]];
    }
}

void KeyExpansion(const uint8_t* key, uint8_t roundKeys[]){
    uint8_t temp[WORD_LEN];
    for (size_t i = 0; i < AES_KEY_LEN; i++)
    {
        roundKeys[i] = key[i];
    }
    
    for(int i = 4; i < 4 * (AES_ROUNDS + 1); i++){
        for(int j = 0; j < WORD_LEN; j++){
            temp[j] = roundKeys[((i - 1)*4) + j ];
        }
        if(i % 4 == 0){
            RotWord(temp);
            SubWord(temp);
            temp[0] = temp[0] ^ Rcon[i / 4];
        }
        for(uint8_t j = 0; j < 4; j++){
            roundKeys[i * 4 + j] = roundKeys[(i - 4) * 4 + j] ^ temp[j];
        }
    }
}

void AddRoundKey(uint8_t* state, const uint8_t* roundKey){
    for(int i = 0; i < AES_BLOCK_SIZE; i++){
        state[i] = state[i] ^ roundKey[i]; 
    }
}

void junkan_shift_left(uint32_t* state, uint8_t target){
    *state = *state << target | (*state >> (32 - target));
}

void junkan_shift_right(uint32_t* state, uint8_t target){
    *state = (*state << (32 - target)) | *state >> target;
}

void SubBytes(uint8_t* state){
    for(int i = 0; i < AES_BLOCK_SIZE; i++){
        state[i] = SBOX[state[i]];
    }
}

void InvSubBytes(uint8_t* state){
    for(int i = 0; i < AES_BLOCK_SIZE; i++){
        state[i] = INV_SBOX[state[i]];
    }
}

void ShiftRows(uint8_t* state){
    uint32_t* ans = (uint32_t*)state;
    for(int i = 3; i >= 0; i--){
        junkan_shift_left(&ans[i], i * 8);
    }
}

void InvShiftRows(uint8_t* state){
    uint32_t* ans = (uint32_t*)state;
    for(int i = 3; i >= 0; i--){
        junkan_shift_right(&ans[i], i * 8);
    }
}


unsigned char gf_mul(unsigned char a, unsigned char b) {
    unsigned char p = 0;
    unsigned char carry;
    for (int i = 0; i < 8; i++) {
        if (b & 1)
            p ^= a;
        carry = a & 0x80;
        a <<= 1;
        if (carry)
            a ^= 0x1b; 
        b >>= 1;
    }
    return p;
}

void InvMixColumns(uint8_t state[4][4]) {
    uint8_t temp[4];
    for (int c = 0; c < 4; c++) {
        for (int i = 0; i < 4; i++) {
            temp[i] = state[i][c];
        }
        
        state[0][c] = gf_mul(0x0e, temp[0]) ^ gf_mul(0x0b, temp[1]) ^ gf_mul(0x0d, temp[2]) ^ gf_mul(0x09, temp[3]);
        state[1][c] = gf_mul(0x09, temp[0]) ^ gf_mul(0x0e, temp[1]) ^ gf_mul(0x0b, temp[2]) ^ gf_mul(0x0d, temp[3]);
        state[2][c] = gf_mul(0x0d, temp[0]) ^ gf_mul(0x09, temp[1]) ^ gf_mul(0x0e, temp[2]) ^ gf_mul(0x0b, temp[3]);
        state[3][c] = gf_mul(0x0b, temp[0]) ^ gf_mul(0x0d, temp[1]) ^ gf_mul(0x09, temp[2]) ^ gf_mul(0x0e, temp[3]);
    }
}


void MixColumns(uint8_t state[4][4]){
    uint8_t tmp[4][4];
    for (int c = 0; c < 4; c++) {
        tmp[0][c] = gf_mul(0x02, state[0][c]) ^ gf_mul(0x03, state[1][c]) ^ state[2][c] ^ state[3][c];
        tmp[1][c] = state[0][c] ^ gf_mul(0x02, state[1][c]) ^ gf_mul(0x03, state[2][c]) ^ state[3][c];
        tmp[2][c] = state[0][c] ^ state[1][c] ^ gf_mul(0x02, state[2][c]) ^ gf_mul(0x03, state[3][c]);
        tmp[3][c] = gf_mul(0x03, state[0][c]) ^ state[1][c] ^ state[2][c] ^ gf_mul(0x02, state[3][c]);
    }
    for (int r = 0; r < 4; r++) {
        for (int c = 0; c < 4; c++) {
            state[r][c] = tmp[r][c];
        }
    }
}


void AES_Encrypt(const uint8_t* in, uint8_t* out, const uint8_t* key){
    uint8_t state[AES_BLOCK_SIZE];
    uint8_t roundKeys[(AES_ROUNDS + 1) * AES_BLOCK_SIZE];

    memcpy(state, in, AES_BLOCK_SIZE);

    KeyExpansion(key, roundKeys);

    AddRoundKey(state, roundKeys);
    
    for(int round = 1; round < AES_ROUNDS; round++){
        SubBytes(state);
        ShiftRows(state);
        MixColumns((unsigned char (*)[4])state);
        AddRoundKey(state, roundKeys + round * AES_BLOCK_SIZE);
    }
    
    SubBytes(state);
    ShiftRows(state);
    AddRoundKey(state, roundKeys + AES_ROUNDS * AES_BLOCK_SIZE);
    memcpy(out, state, AES_BLOCK_SIZE);
}
void AES_Decrypt(const uint8_t* in, uint8_t* out, const uint8_t* key) {
    uint8_t state[AES_BLOCK_SIZE];
    uint8_t roundKeys[(AES_ROUNDS + 1) * AES_BLOCK_SIZE];

    memcpy(state, in, AES_BLOCK_SIZE);


    KeyExpansion(key, roundKeys);


    AddRoundKey(state, roundKeys + AES_ROUNDS * AES_BLOCK_SIZE);


    for (int round = AES_ROUNDS - 1; round > 0; round--) {
        InvShiftRows(state);
        InvSubBytes(state);
        AddRoundKey(state, roundKeys + round * AES_BLOCK_SIZE);
        InvMixColumns((uint8_t (*)[4])state); 
    }
    InvShiftRows(state);
    InvSubBytes(state);
    AddRoundKey(state, roundKeys);
    
    memcpy(out, state, AES_BLOCK_SIZE);
}

char* memorydump(char* buf, uint8_t* array, int len){
    for(int i = 0; i < len; i++){
        sprintf(buf + (i * 6), "0x%02hx, ", array[i]);
    }
    return buf;
}
char* textPrint(char* buf, uint8_t* text, int len){
    for(int i = 0; i < len; i++){
        buf[i] = text[i];
    }
    buf[len] = '\0';
    return buf;
}
// Example usage
int main() {
    uint8_t key[AES_KEY_LEN] = "AES_Mikan_26980";
    uint8_t plaintext[AES_BLOCK_SIZE] = "Hello world AES";
    uint8_t ciphertext[AES_BLOCK_SIZE];
    uint8_t fukugoutext[AES_BLOCK_SIZE];
    char buf[1024];

    AES_Encrypt(plaintext, ciphertext, key);
    AES_Decrypt(ciphertext, fukugoutext, key);
    printf("text = %s\n", textPrint(buf, plaintext, AES_BLOCK_SIZE));
    printf("memory = %s\n", memorydump(buf, plaintext, AES_BLOCK_SIZE));
    printf("text = %s\n", textPrint(buf, ciphertext, AES_BLOCK_SIZE));
    printf("memory = %s\n", memorydump(buf, ciphertext, AES_BLOCK_SIZE));
    printf("text = %s\n", textPrint(buf, fukugoutext, AES_BLOCK_SIZE));
    printf("memory = %s\n", memorydump(buf, fukugoutext, AES_BLOCK_SIZE));
    return 0;
}